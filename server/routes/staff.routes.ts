import type { Express } from "express";

export function registerStaffRoutes(_app: Express) {
  // Staff currently uses the shared owner/staff turf-management endpoints.
  // Add staff-only endpoints here when the role gets unique workflows.
}
